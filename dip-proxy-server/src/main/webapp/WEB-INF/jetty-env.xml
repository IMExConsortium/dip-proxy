<?xml version="1.0"?>
<!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure//EN" "http://jetty.mortbay.org/configure.dtd">

<Configure class="org.mortbay.jetty.webapp.WebAppContext">

 <New class="org.mortbay.jetty.plus.naming.Resource">
  <Arg>jdbc/proxyws</Arg>
  <Arg>
   <New class="org.postgresql.ds.PGSimpleDataSource">
    <Set name="User">${dip-proxy.hibernate.connection.username}</Set>
    <Set name="Password">${dip-proxy.hibernate.connection.password}</Set>
    <Set name="DatabaseName">${dip-proxy.hibernate.connection.dbname}</Set>
    <Set name="ServerName">${dip-proxy.hibernate.connection.servername}</Set>
    <Set name="PortNumber">${dip-proxy.hibernate.connection.portnum}</Set>
   </New>
  </Arg>
 </New>
</Configure>

