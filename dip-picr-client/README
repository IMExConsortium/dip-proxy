Goal: This projet is used to generate EBI PICR webservice client and its jar file.

SearchDB: REFSEQ, SWISSPROT, PDB

InputFile format: inputFile contains refseq protein accession and each line contains one accession.

Command:
    mvn package
        That can build a picr-client.jar file located in target directory.

    java -cp path_to_jar/picr-client.jar edu.ucla.mbi.proxy.ebi.PicrClient inputFile outputFile
        That is client command. The command contains two arguments, one is input file, another is output file.

